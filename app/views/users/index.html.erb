<% provide(:title, 'Nutzer') %>
<div class="row">
  <aside class="col-md-2">
  </aside>
  <div class="col-md-10">
  	<h1 class="h1_formular">Alle Nutzer</h1>
	<ul class="users">
	 <%= render @users %>
	</ul>
	<%= will_paginate %>
  </div>
</div>
